{
  "name": "kataribe-monorepo",
  "version": "0.1.0",
  "description": "Bidirectional (client↔server) RPC + events over WebSocket with type-safe unknown-only envelopes.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "npm run clean --workspaces",
    "format": "biome check --write --unsafe",
    "lint": "biome lint",
    "check": "biome ci",
    "test": "npm test --workspaces",
    "dev:server": "tsx examples/nodejs-ws/server.ts",
    "dev:client": "tsx examples/nodejs-ws/client.ts",
    "dev:webrtc-signaling": "tsx examples/nodejs-wrtc/signaling-server.ts"
  },
  "keywords": [
    "rpc",
    "websocket",
    "typescript",
    "bidirectional",
    "events",
    "envelope",
    "monorepo"
  ],
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "@types/node": "^24.5.2",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.25.10",
    "tsx": "^4.20.5",
    "typescript": "~5.9.2"
  }
}
